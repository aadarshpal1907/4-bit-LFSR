# 4-bit-LFSR
This project focuses on the implementation of a 4-bit Linear Feedback Shift Register (LFSR) based on the characteristic polynomial  𝑥^4 + 𝑥 + 1. The LFSR generates a pseudo-random sequence by shifting bits and applying feedback through XOR logic as defined by the polynomial.
